---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Super 8 | discontinuity"
  description="Super 8 motion film by discontinuity. Analog moving images on celluloid."
  currentPage="super8"
  showLightbox={true}
>
  <header class="page-header super8">
    <h1>SUPER 8</h1>
    <p>Motion Film</p>
  </header>

  <section class="info-block">
    <p>Super 8 is motion picture film at its most intimate. The texture, the warmth, the soft flicker of celluloid in motion&mdash;it captures time the way memory feels. Each cartridge holds three minutes and twenty seconds. No second takes. Just light through a gate and chemistry doing the rest.</p>
  </section>

  <section class="video-grid" aria-label="Super 8 reels">
    {
      [
        { id: '-FcJp-N7r4k', title: 'Reel 01' },
        { id: 'pVrz0t1XTAw', title: 'Reel 02' },
        { id: 'FrWyn99WR90', title: 'Reel 03' },
        { id: 'dJwZ_MMfmUo', title: 'Reel 04' },
        { id: 'e0t4Pe7a-Hs', title: 'Reel 05' },
      ].map((video) => (
        <div class="video-item" tabindex="0" onclick={`window.openLightbox('video', null, '${video.title}', '${video.id}')`}>
          <div class="video-thumbnail-container">
            <img src={`https://img.youtube.com/vi/${video.id}/hqdefault.jpg`} alt={`Thumbnail for ${video.title}`} />
            <div class="video-play-btn">PLAY</div>
          </div>
          <div class="video-title">{video.title}</div>
        </div>
      ))
    }
  </section>
